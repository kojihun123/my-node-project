1️⃣ **CRUD 기능부터 만들기**
→ `User`, `Post` 모델로 Create / Read / Update / Delete API 구축
→ Express + Prisma로 백엔드 완성

2️⃣ **React에서 API 연동**
→ 프론트에서 `fetch("/api/users")` 등으로 데이터 표시
→ 간단한 UI로 리스트 / 추가 / 삭제 기능 구현

3️⃣ **기능 확장 & 다듬기**
→ JWT 로그인 추가
→ TailwindCSS로 UI 개선
→ Render(Vercel) 배포
